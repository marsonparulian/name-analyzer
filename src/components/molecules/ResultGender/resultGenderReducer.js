// <ResultGender /> state reducer

const reducer = (state = null, action) => {
    let newState;
    switch (action.type) {

        default:
            return state;
    }
}
export default reducer;
